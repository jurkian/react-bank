{
  "routes": [
    {
      "method": "GET",
      "path": "/cards",
      "handler": "card.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cards/count",
      "handler": "card.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cards/:id",
      "handler": "card.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/cards",
      "handler": "card.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/cards/:id",
      "handler": "card.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/cards/:id",
      "handler": "card.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
