{
  "routes": [
    {
      "method": "GET",
      "path": "/transfers",
      "handler": "transfer.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/transfers/count",
      "handler": "transfer.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/transfers/:id",
      "handler": "transfer.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/transfers",
      "handler": "transfer.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/transfers/:id",
      "handler": "transfer.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/transfers/:id",
      "handler": "transfer.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
