{
  "routes": [
    {
      "method": "GET",
      "path": "/app/config",
      "handler": "common.getAppConfig",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/app/generate-dummy-data",
      "handler": "common.generateData",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/app/clean-all-data",
      "handler": "common.cleanAllData",
      "config": {
        "policies": []
      }
    }
  ]
}
