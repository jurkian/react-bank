{
  "routes": [
    {
      "method": "GET",
      "path": "/app/config",
      "handler": "common.getAppConfig",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/app/generate-dummy-data",
      "handler": "common.generateDummyData",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/app/clean-all-data",
      "handler": "common.cleanAllData",
      "config": {
        "policies": []
      }
    }
  ]
}
