import React from 'react';
import PropTypes from 'prop-types';

import './style.css';

const CurrencySettings = (props) => {
   return (
      <div>
         Choose currencies and date...
      </div>
   );
}

CurrencySettings.propTypes = {}

export default CurrencySettings;